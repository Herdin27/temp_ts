export interface ResponseTestData {
    no: number,
    no_order: string,
    tanggal: string,
    id_karyawan: number,
    status: number,
    no_mobil: string
}

export interface RequestTestData {
    foo: number
    bar?: string
}